import { MediaElement } from "../media-element.js";
import { PlayerController } from "../../player/player-controller.js";
import { index_d_exports } from "../../index.js";
import { PropertyDeclarationMap, PropertyValues } from "@videojs/element";
import { State } from "@videojs/store";
import * as _$_videojs_core0 from "@videojs/core";
import { LiveButtonCore } from "@videojs/core";

//#region src/ui/live-button/live-button-element.d.ts
/**
 * `<media-live-button>` — selects from `live`, `time`, and `buffer` features
 * and composes them into the `LiveButtonMediaState` consumed by
 * `LiveButtonCore`.
 *
 * Doesn't extend `MediaButtonElement` because that base couples a button to
 * a single feature selector; the LiveButton needs three.
 */
declare class LiveButtonElement extends MediaElement {
  #private;
  static readonly tagName = "media-live-button";
  static properties: PropertyDeclarationMap;
  disabled: boolean;
  label: string;
  protected readonly core: LiveButtonCore;
  protected readonly live: PlayerController<index_d_exports.AnyPlayerStore, _$_videojs_core0.MediaLiveState | undefined>;
  protected readonly time: PlayerController<index_d_exports.AnyPlayerStore, _$_videojs_core0.MediaTimeState | undefined>;
  protected readonly buffer: PlayerController<index_d_exports.AnyPlayerStore, _$_videojs_core0.MediaBufferState | undefined>;
  get $state(): State<LiveButtonCore.State>;
  connectedCallback(): void;
  disconnectedCallback(): void;
  /** Returns the button's current label derived from media state. */
  getLabel(): string | undefined;
  protected willUpdate(changed: PropertyValues): void;
  protected update(changed: PropertyValues): void;
}
//#endregion
export { LiveButtonElement };
//# sourceMappingURL=live-button-element.d.ts.map