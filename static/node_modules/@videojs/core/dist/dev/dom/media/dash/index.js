import { HTMLVideoElementHost } from "../video-host.js";
import * as dashjs from "dashjs";
//#region src/dom/media/dash/index.ts
const dashMediaDefaultProps = { src: "" };
var DashMedia = class extends HTMLVideoElementHost {
	#engine;
	#src = dashMediaDefaultProps.src;
	constructor() {
		super();
		this.#engine = dashjs.MediaPlayer().create();
		this.#engine.initialize(void 0, void 0, false);
	}
	get engine() {
		return this.#engine;
	}
	get src() {
		return this.#src;
	}
	set src(src) {
		this.#src = src;
		this.#engine.attachSource(src);
	}
	attach(target) {
		super.attach(target);
		this.#engine.attachView(target);
	}
	detach() {
		super.detach();
		this.#engine.attachView(null);
	}
	destroy() {
		this.detach();
		this.#engine.destroy();
	}
};
//#endregion
export { DashMedia, dashMediaDefaultProps };

//# sourceMappingURL=index.js.map