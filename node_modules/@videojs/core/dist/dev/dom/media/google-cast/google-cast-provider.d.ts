import { RemotePlaybackState } from "./remote-playback.js";
import { GoogleCastMediaElement } from "./types.js";

//#region src/dom/media/google-cast/google-cast-provider.d.ts
type GoogleCastProviderHooks = {
  setState: (next: RemotePlaybackState) => void;
  setAvailable: (available: boolean) => void;
};
type LocalPlayer = {
  duration: () => number;
  currentTime: () => number;
  paused: () => boolean;
  muted: () => boolean;
  pause: () => void;
};
declare class GoogleCastProvider {
  #private;
  readonly media: GoogleCastMediaElement;
  seeking: boolean;
  constructor(media: GoogleCastMediaElement, local: LocalPlayer);
  bindHooks(hooks: Partial<GoogleCastProviderHooks>): void;
  get isCasting(): boolean;
  set isCasting(value: boolean);
  hasDevicesAvailable(): boolean;
  requestCastSession(): Promise<void>;
  load(): Promise<void>;
  play(): Promise<void>;
  pause(): void;
  get paused(): any;
  get ended(): boolean;
  get muted(): boolean;
  set muted(value: boolean);
  get volume(): number;
  set volume(value: number);
  get playbackRate(): number;
  set playbackRate(value: number);
  get duration(): any;
  get currentTime(): number;
  set currentTime(value: number);
  get readyState(): 0 | 2 | 3;
  init(): void;
  onCastStateChanged(): void;
  onSessionStateChanged(): Promise<void>;
  destroy(): void;
}
//#endregion
export { GoogleCastProvider };
//# sourceMappingURL=google-cast-provider.d.ts.map